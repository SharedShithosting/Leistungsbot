# Run this job on pushes to `main`, and for pull requests. If you don't specify
# `branches: [main], then this actions runs _twice_ on pull requests, which is
# annoying.
name: pytest
on:
  push:
    branches: [main]
  pull_request:
jobs:
  test:
    runs-on: ubuntu-latest
    environment:
      name: My Environment
    env:
      # this will take the entire vars context configured in the repository > environments
      # setting and convert it to JSON so we can automatically assign all the values to the
      # runner's environment
      VARS_CONTEXT: ${{ toJson(vars) }}
      SECRETS_CONTEXT: ${{ toJson(secrets) }}
    steps:
      - uses: actions/checkout@v3
      # If you wanted to use multiple Python versions, you'd have specify a matrix in the job and
      # reference the matrixe python version here.
      - uses: actions/setup-python@v5
        with:
          python-version: 3.11
      # Install Poetry. You could do this manually, or there are several actions that do this.
      # `snok/install-poetry` seems to be minimal yet complete, and really just calls out to
      # Poetry's default install script, which feels correct. I pin the Poetry version here
      # because Poetry does occasionally change APIs between versions and I don't want my
      # actions to break if it does.
      #
      # The key configuration value here is `virtualenvs-in-project: true`: this creates the
      # venv as a `.venv` in your testing directory, which allows the next step to easily
      # cache it.
      - uses: snok/install-poetry@v1
        with:
          version: 2.0.0
          virtualenvs-create: true
          virtualenvs-in-project: true
      # Install dependencies. `--no-root` means "install all dependencies but not the project
      # itself", which is what you want to avoid caching _your_ code. The `if` statement
      # ensures this only runs on a cache miss.
      - run: poetry install --no-interaction --no-root --with=dev
        if: steps.cache-deps.outputs.cache-hit != 'true'
      # Now install _your_ project. This isn't necessary for many types of projects -- particularly
      # things like Django apps don't need this. But it's a good idea since it fully-exercises the
      # pyproject.toml and makes that if you add things like console-scripts at some point that
      # they'll be installed and working.
      - run: poetry install --no-interaction
      - name: Set env vars from vars context JSON
        run: |
          # Pipe the JSON string into jq
          echo "$VARS_CONTEXT" |
          # Convert JSON object into an array of key-value pairs
          jq -r 'to_entries |
          # Map over each key-value pair
          .[] |
          # Format each pair as "KEY=VALUE" and append it all to the environment file
          "\(.key)=\(.value)"' >> $GITHUB_ENV
      - name: Set env vars from secrets context JSON
        run: |
          # Pipe the JSON string into jq
          echo "$SECRETS_CONTEXT" |
          # Convert JSON object into an array of key-value pairs
          jq -r 'to_entries |
          # Map over each key-value pair
          .[] |
          # Format each pair as "KEY=VALUE" and append it all to the environment file
          "\(.key)=\(.value)"' >> $GITHUB_ENV
      # And finally run tests. I'm using pytest and all my pytest config is in my `pyproject.toml`
      # so this line is super-simple. But it could be as complex as you need.
      - run: poetry run pytest --junitxml=reports/junit/junit.xml
      # Generate badges
      - run: poetry run genbadge tests -o reports/badges/tests-badge.svg
      # Run coverage tests
      - run: poetry run coverage run -m pytest
      - run: poetry run coverage report -m
      - run: poetry run coverage xml -o reports/coverage/coverage.xml
      - run: poetry run coverage html -d reports/coverage
      - run: poetry run coverage json -o reports/coverage/coverage.json
      # Every file has to clear 75%, not just the project as a whole - a
      # module with no tests at all hides behind a good total otherwise.
      - name: Per file coverage gate
        run: poetry run python tools/check_coverage.py reports/coverage/coverage.json
      # Readable without downloading anything: this lands on the run's
      # summary page, under the job list.
      - name: Coverage summary
        if: always()
        run: |
          {
            echo "## Coverage"
            echo
            echo '```'
            # `|| true`: coverage report exits nonzero under fail_under, and
            # the summary should still render when it does. The gate above
            # is what fails the build.
            poetry run coverage report -m || true
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"
      # Generate badges
      - run: poetry run genbadge coverage -o reports/badges/coverage-badge.svg
      # lint report. genbadge can only read flake8's statistics format, and
      # ruff does not produce it, so this is a report without a badge.
      - run: |
          mkdir -p reports/ruff
          poetry run ruff check leistungsbot \
            --output-format=github --exit-zero | tee reports/ruff/ruff.txt
      # Uploaded on every run, not just main. The html report is only worth
      # generating if it can be fetched from the pull request that changed
      # the numbers; before, it was built and thrown away.
      # Download it from the run page, unzip, open coverage/index.html.
      # Publishing it somewhere clickable is #39.
      - name: Archive reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report-${{ github.run_id }}
          path: reports
          retention-days: 14
